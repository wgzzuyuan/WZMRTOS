1，任务调度的关键词：   任务状态，任务控制块，就虚表，调度器上锁，任务调度，任务级的任务调度

关键词的抽象：任务控制块，调度器上锁，任务级别的任务调度
关键词的抽象原则：任务状态，任务控制块，就虚表，这3个可以抽象为 任务控制块。
                任务调度，任务级的任务调度可以抽象为更具体的任务级别的任务调度。
                
任务调度的前提是创建任务

任务调度涉及任务的状态，在信号量，消息，事件标志，延时函数等外部条件变换时，引起任务调度

任务控制块，这个关键词是任务调度的最核心的数据结构，不仅仅是任务调度，信号量，消息等引起的任务间通信的时候，也会操作这个数据结构，这个数据结构是OS
运行过程中的重中之重。********************任务创建的时候，初始化这个数据结构。
面试中的查表法的实现，和任务控制块息息相关，优先级也和任务控制块息息相关，双向链表也和任务控制块息息相关，

就虚表，这个是个关键词，查表的方法被专门拿出来一节进行行讲解。

2，进一步提取和总结，进一步抽象，把任务创建函数做的事情忽略，把信号量，消息，事件等外部条件引起的调度过程忽略，把中断级别的任务调度引起的调度忽略，
这里只考虑任务级的任务调度和调度器上锁对应的这个过程。


3, ucosii的任务级的任务调度的过程

关中断

  条件成立：无中断嵌套 | 调度器没有上锁
  
  获取最高优先级
  
    条件成立：当前正在运行的任务的优先级不是最高优先级
  
    把最高优先级赋值给任务控制块
  
    任务切换计数器加一
  
    执行 OS_TASK_SW()任务级的任务调度

开中断






